# .readthedocs.yaml
# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration: source/conf.py

# Optionally, add these if you want to build docs for multiple modules or set custom build commands
# build:
#   jobs:
#     pre_build:
#       - echo "Pre-build step"
#     post_build:
#       - echo "Post-build step"

# Optionally, add environment variables
# environment:
#   - name: EXAMPLE_VAR
#     value: example
